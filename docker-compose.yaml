version: '3'
services:
  app:
    container_name: server-fantasy-11
    restart: always
    build: .
    ports:
      - '4000:4000'
    links:
      - mongo
  mongo:
    container_name: mongo-db-fantasy-11
    image: mongo
    ports:
      - '27017:27017'
